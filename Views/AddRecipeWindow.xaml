<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views" xmlns:viewmodels="clr-namespace:CookMaster.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AddRecipeViewModel}"
        mc:Ignorable="d"
        Title="AddRecipeWindow" Height="450" Width="800" FontFamily="Maiandra GD" FontSize="16" Background="PaleTurquoise">
    <Grid>
        <StackPanel>
            <Label Grid.Column="0" Grid.ColumnSpan="3" Content="Cook Master" FontSize="34" />
            <TextBlock Text="Add New Recipe" FontSize="22" Margin="40,20,0,20" />
            <TextBlock Text="Title:" Margin="40,0,40,5"/>
            <TextBox Text="{Binding Title}" Margin="40,0,40,10"/>
            <TextBlock Text="Ingredients:" Margin="40,0,40,10"/>
            <TextBox Text="{Binding Ingredients}" Margin="40,0,40,10"/>
            <TextBlock Text="Instructions:" Margin="40,0,40,5"/>
            <TextBox Text="{Binding Instructions}" Margin="40,0,40,10"/>
            <TextBlock Text="Category:" Margin="40,0,40,5"/>
            <TextBox Text="{Binding Category}" Margin="40,0,40,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="5" FontSize="12" Width="70" Height="26" />
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5" FontSize="12" Width="70" Height="26" />
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
